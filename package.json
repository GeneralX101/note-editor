{
  "name": "note-editor",
  "version": "1.0.0",
  "description": "Note editing interfaces for Editors' Notes",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && browserify -o dist/bundle.js index.js",
    "watch": "mkdir -p dist && watchify -dv -o dist/bundle.js index.js"
  },
  "author": "Patrick Golden",
  "license": "MIT",
  "dependencies": {
    "backbone": "^1.1.2",
    "browserify": "^8.1.1",
    "jquery": "^2.1.3",
    "quill": "^0.19.8",
    "remarkable": "^1.6.0",
    "watchify": "^2.2.1"
  }
}
